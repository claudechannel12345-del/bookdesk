Build the app specified in SPEC.md in this repo (read it fully first and follow it exactly — especially "Claude integration", "Writing Rules", and "Required verification").

Current state: electron-vite + React + TS scaffold (commit ff1a000) plus substantial UNCOMMITTED work in the working tree from a previous agent that was interrupted mid-build (backend services and most UI exist; it was styling the diff modal when stopped). Audit the working tree first (git status/diff), keep what matches the spec, finish or rewrite what doesn't. Commit in sensible increments from here on.

Environment:
- Windows 11. Dev target: `npm start` (or `npm run dev`) must work HERE. Mac is the eventual package target — electron-builder mac dmg config only, do not attempt a mac build.
- The `claude` CLI is installed and authenticated on this machine. Use it for real end-to-end verification (spec step 3): send an actual message through your app's session manager asking Claude to edit a chapter file; confirm the file changes, the editor live-reloads, and the review strip + Undo work. Use `--model sonnet` for test turns; the app's shipped default stays `sonnet[1m]`.
- Do NOT invoke the `codex` CLI itself during testing — verify the Codex-button path only by confirming the spawn code handles a missing/present binary gracefully.
- If npm hits TLS errors (AV proxy), set NODE_OPTIONS=--use-system-ca.

Deliverables:
1. Working app per spec.
2. VERIFIED.md — honest observed results of the 4 verification steps in the spec (commands run, what actually happened). Honest failures beat claimed success.
3. Final summary: what works, what's stubbed, deviations from spec with reasons.
